# ============================================================================
# Atlas configuration. Copy this file to `.env` and fill in your key:
#     cp .env.example .env
# Every line except ANTHROPIC_API_KEY is optional — sensible defaults are shown.
# If you leave a setting blank, remove its trailing "# comment" too
# (don't leave e.g.  ATLAS_WORKDIR=   # ...).
# ============================================================================

# REQUIRED — your Anthropic API key. Powers BOTH Claude agents:
#   1. the formatting agent (turns your speech into a clean Cursor prompt)
#   2. the summary agent    (turns Cursor's output into a spoken reply)
# Both run on Claude Opus 4.8.  Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# Claude agents (optional)
# ---------------------------------------------------------------------------
# ATLAS_MODEL=claude-opus-4-8
# ATLAS_FORMAT_THINKING=0     # 1 = let Claude think before formatting (higher quality, slower)
# ATLAS_EFFORT=medium         # low | medium | high | max  (only used when thinking is on)

# ---------------------------------------------------------------------------
# Cloud history + directory memory (Convex) — optional.
# Run:  cd backend && npm install && npx convex dev   to get a deployment URL.
# With this set, Atlas stores every run and remembers your active project dir, so
# you can say "build on my last project" or "switch to ~/projects/foo".
# ---------------------------------------------------------------------------
# CONVEX_URL=https://your-project-123.convex.cloud
# CONVEX_TOKEN=               # optional auth token if your deployment requires one

# ---------------------------------------------------------------------------
# "Voice Cursor" — the Cursor CLI agent that does the real work.
# Install it from https://cursor.com/cli and authenticate once:
#     cursor-agent login        (or set CURSOR_API_KEY below)
# To run Cursor on Opus 4.8: add your Anthropic key inside the Cursor app and
# pick Opus 4.8 as the model, OR set ATLAS_CURSOR_MODEL to the exact id your
# Cursor exposes for Opus 4.8.
# ---------------------------------------------------------------------------
# ATLAS_CURSOR_COMMAND=cursor-agent
# ATLAS_CURSOR_MODEL=         # blank = use Cursor's selected model
# CURSOR_API_KEY=            # optional: Cursor CLI token (alternative to `cursor-agent login`)
# ATLAS_WORKDIR=             # project folder Cursor works in (defaults to current dir)
# ATLAS_CURSOR_FORCE=        # blank = auto (allow edits/terminal only when intended); 1 = always; 0 = never
# ATLAS_CURSOR_TIMEOUT=600   # seconds to wait for Cursor

# ---------------------------------------------------------------------------
# Voice input (speech-to-text)
# ---------------------------------------------------------------------------
# STT_BACKEND=google         # google (no setup, needs internet) | whisper (offline, private)
# ATLAS_WHISPER_MODEL=base.en
# ATLAS_PAUSE_THRESHOLD=0.8  # seconds of silence that ends a phrase
# ATLAS_PHRASE_TIME_LIMIT=30 # max seconds for one spoken command
# ATLAS_ENERGY_THRESHOLD=    # fixed mic sensitivity; blank = auto-calibrate
# ATLAS_MIC_INDEX=           # input device number from `python run.py --mic-test`; blank = system default

# ---------------------------------------------------------------------------
# Voice output (macOS `say`)
# ---------------------------------------------------------------------------
# ATLAS_TTS_VOICE=           # e.g. Samantha, Daniel, Alex   (run `say -v ?` to list)
# ATLAS_TTS_RATE=            # words per minute, e.g. 190
# ATLAS_SPEAK_PREFACE=1      # speak a short "Working on it" once your command is understood
