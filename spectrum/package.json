{
  "name": "atlas-spectrum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Photon Spectrum iMessage gateway for Atlas — bridges voice memos to the Convex command queue.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.17.0",
    "dotenv": "^16.4.5",
    "spectrum-ts": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
