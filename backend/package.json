{
  "name": "atlas-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Convex backend for Atlas — global run history + active-directory memory.",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.17.0"
  }
}
